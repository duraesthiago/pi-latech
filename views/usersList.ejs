<%- include('partials/head') %>

  <!-- Include Header -->
  <%- include('partials/headerAdmin') %>

    <main>
      <link rel="stylesheet" href="/stylesheets/adminPanel.css" />

      <h3 class="edit_title">Painel de Administrador - Lista de Usuários</h3>

      <div class="edit-container">
        <h2>Lista de usuários</h2>
        <div>
            <ul>
                <% for (user of usersList) {%>
                <a href="">
                <li><%= user.Nome %>,  <%= user.Sobrenome %>,  <%= user.Email %>, <%= user.Cpf %>, <%= user.Telefone %>, <%= user.Avatar%>, <%= user.Senha%></li>
            </a>
            <% } %>
            </ul>
        </div>

        <form action="#">
          <button class="btn-admin-edit-product">Editar usuário</button>
        </form>

        <form action="#"">
          <button class="btn-admin-edit-user" type="submit">Excluir usuário</button>
        </form>
      </div>
    </main>
    <!-- Include Footer -->
    <%- include('partials/footer') %>