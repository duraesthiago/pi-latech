<%- include('partials/head') %>

<%- include('partials/header', {title: 'Inicio'}) %>

<main>
    <link rel="stylesheet" href="/stylesheets/updateUser.css"> 
    <br>
    <h2 class="edit-title">Editar Conta</h2>
    <br>
    <div class="account-container">
        <div class="user-info-container">
            <h3>Olá, <%= userLogged.Nome %></h3>
            <form 
            class="user-info-form"
            method="post" 
            action="/users/updateUserData/ <%= userLogged.idUser%>/update?_method=PUT"
            >
            <div class="user-avatar">
                <img src="<%= userLogged.Avatar %>" alt="">
                <button class="btn-update-avatar"></button>
            </div>
            <div class="user-form">
                <div class="user-form-class">
                    <label class="form-label" for="nome">Nome do Usuário</label>
                    <input
                    class="input-label"
                    type="text"
                    id="nome"
                    name="name"
                    value="<%= userLogged.Nome %>"
                    />
                </div>
                <div class="user-form">
                    <div class="user-form-class">
                        <label class="form-label" for="nome">Sobrenome do Usuário</label>
                        <input
                        class="input-label"
                        type="text"
                        id="lastNamee"
                        name="lastName"
                        value="<%= userLogged.Sobrenome %>"
                        />
                    </div><div class="user-form">
                        <div class="user-form-class">
                            <label class="form-label" for="nome">Nome do Usuário</label>
                            <input
                            class="input-label"
                            type="text"
                            id="telefone"
                            name="phone"
                            value="<%= userLogged.Telefone %>"
                            />
                        </div>
                        <button class="btn-save" type="submit">Salvar</button>
            </div>
        </form>
        </div>
    </div>
</main>
<%- include('partials/footer') %>