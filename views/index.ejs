<!-- Include Head -->
<%- include('partials/head') %>
  <!-- Include Header -->
  <%- include('partials/header', {title: 'Início' }) %>
    <!-- Include navbarCategories -->
    <%- include('partials/navbarCategories') %>
      <!-- Include Slide-->
      <%- include('partials/slide') %>
        <main class="index-main">
          <div class="products-container">
            <h2 class="index-title">PRODUTOS EM DESTAQUE</h2>
            <div class="products-wrapper">
              <% for(let product of products){ %>
                <div class="products-box">
                  <div>
                    <a href="/products/detail/<%= product.idProdutos %>">
                      <img src="<%= product['images.Imagem'] %>" alt="<%= product.Nome%>" class="product-img" />
                      <div class="product-description">
                        <%= product.Nome %>
                      </div>
                      <div class="products-old-price" id="products-old-price">R$ <%= product.Preco %>
                      </div>
                      <div class="products-price" id="products-price">R$ <%= product.PrecoComDesconto %>
                      </div>
                      <form action="/products/detail/<%= product.idProdutos %>" method="GET">
                        <button class="btn-detail" type="submit">VER MAIS
                    </a>
                    </Button>
                    </form>
                    </a>
                  </div>
                </div>
                <% } %>
            </div>
            <!-- Include Slide-->
            <%- include('partials/indexOfferProduct') %>            
          </div>
          </div>
        </main>
        <!-- Include Footer -->
        <%- include('partials/footer') %>